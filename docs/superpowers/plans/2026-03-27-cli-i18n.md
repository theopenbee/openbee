# CLI i18n 多语言支持实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 openbee CLI 添加多语言支持（zh/en），通过嵌入式 YAML + 类型化结构体实现，语言优先级为 `--lang` flag → `OPENBEE_LANG` 环境变量 → `config.yaml language` 字段 → 默认 zh。

**Architecture:** 新建 `internal/i18n` 包，包含嵌入式 YAML 文件（`locales/zh.yaml`、`locales/en.yaml`）和类型化 `Messages` 结构体。`main()` 在 `Execute()` 之前解析语言、加载翻译、并通过 `applyTranslations()` 覆写所有 cobra 命令的 `Short`/`Long` 字段。survey 提示文字在命令执行时直接从 `i18n.M` 读取。

**Tech Stack:** Go 1.25, `gopkg.in/yaml.v3`（已在 go.mod 中）, `embed` 标准库, `github.com/spf13/cobra`, `github.com/AlecAivazis/survey/v2`

---

## 文件一览

**新建：**
- `internal/i18n/messages.go` — Messages 结构体定义（所有 CLI 文本的类型化字段）
- `internal/i18n/i18n.go` — `Load(lang string) error`、`M *Messages`、embed 声明
- `internal/i18n/locales/zh.yaml` — 中文翻译（默认语言，必须完整）
- `internal/i18n/locales/en.yaml` — 英文翻译（当前 CLI 硬编码文本）
- `internal/i18n/i18n_test.go` — 单元测试
- `cmd/openbee/lang.go` — `parseLangFlag(args []string) string` 和 `detectLang(flagLang string) string`

**修改：**
- `internal/config/config.go` — 在 `Config` struct 加 `Language string` 字段，新增 `GetLang(path string) string` 函数
- `cmd/openbee/main.go` — 注册 `--lang` persistent flag，在 `Execute()` 前调用 `i18n.Load()` 和 `applyTranslations()`
- `cmd/openbee/config.go` — survey `Message` 字符串替换为 `i18n.M.Prompt.*`
- `cmd/openbee/config_claude.go` — survey `Message` 字符串替换为 `i18n.M.Prompt.*`
- `cmd/openbee/server.go` — `Short` 字段留空（由 `applyTranslations()` 赋值）
- `cmd/openbee/stop.go` — 同上
- `cmd/openbee/restart.go` — 同上
- `cmd/openbee/status.go` — 同上
- `cmd/openbee/upgrade.go` — 同上
- `cmd/openbee/claude.go` — 同上（三个命令）

---

## Task 1: 核心 i18n 包 — 结构体 + YAML + 加载逻辑

**Files:**
- Create: `internal/i18n/messages.go`
- Create: `internal/i18n/i18n.go`
- Create: `internal/i18n/locales/zh.yaml`
- Create: `internal/i18n/locales/en.yaml`
- Create: `internal/i18n/i18n_test.go`

- [ ] **Step 1: 写失败测试**

新建 `internal/i18n/i18n_test.go`：

```go
package i18n_test

import (
	"testing"

	"github.com/theopenbee/openbee/internal/i18n"
)

func TestLoad_zh(t *testing.T) {
	if err := i18n.Load("zh"); err != nil {
		t.Fatalf("Load zh: %v", err)
	}
	if i18n.M == nil {
		t.Fatal("M is nil after Load")
	}
	if i18n.M.Cmd.Root.Short == "" {
		t.Error("Cmd.Root.Short is empty for zh")
	}
	if i18n.M.Prompt.ServerPort == "" {
		t.Error("Prompt.ServerPort is empty for zh")
	}
}

func TestLoad_en(t *testing.T) {
	if err := i18n.Load("en"); err != nil {
		t.Fatalf("Load en: %v", err)
	}
	if i18n.M.Cmd.Root.Short != "OpenBee core service" {
		t.Errorf("Cmd.Root.Short: got %q, want %q", i18n.M.Cmd.Root.Short, "OpenBee core service")
	}
	if i18n.M.Prompt.ServerPort != "Server port:" {
		t.Errorf("Prompt.ServerPort: got %q, want %q", i18n.M.Prompt.ServerPort, "Server port:")
	}
}

func TestLoad_unsupported_fallbacks_to_zh(t *testing.T) {
	// 加载 zh 作为基准
	if err := i18n.Load("zh"); err != nil {
		t.Fatalf("Load zh: %v", err)
	}
	zhShort := i18n.M.Cmd.Root.Short

	// 加载不支持的语言，应 fallback 到 zh
	if err := i18n.Load("fr"); err != nil {
		t.Fatalf("Load fr (fallback): %v", err)
	}
	if i18n.M.Cmd.Root.Short != zhShort {
		t.Errorf("fallback: got %q, want zh value %q", i18n.M.Cmd.Root.Short, zhShort)
	}
}

func TestSupportedLangs(t *testing.T) {
	if len(i18n.SupportedLangs) < 2 {
		t.Errorf("SupportedLangs: expected at least 2, got %d", len(i18n.SupportedLangs))
	}
}
```

- [ ] **Step 2: 运行测试，确认失败（包不存在）**

```bash
cd /path/to/openbee && go test ./internal/i18n/...
```

预期：`cannot find package` 或 `build failed`

- [ ] **Step 3: 新建 `internal/i18n/messages.go`**

```go
package i18n

// Messages 包含所有 CLI 用户可见文本。
// 字段名通过 yaml tag 与 YAML 键对应。
type Messages struct {
	Cmd    CmdMessages    `yaml:"cmd"`
	Prompt PromptMessages `yaml:"prompt"`
}

// CmdMessages 对应所有 cobra 命令的 Short/Long 描述。
type CmdMessages struct {
	Root struct {
		Short string `yaml:"short"`
	} `yaml:"root"`
	Config struct {
		Short string `yaml:"short"`
	} `yaml:"config"`
	Server struct {
		Short string `yaml:"short"`
	} `yaml:"server"`
	Stop struct {
		Short string `yaml:"short"`
	} `yaml:"stop"`
	Restart struct {
		Short string `yaml:"short"`
	} `yaml:"restart"`
	Status struct {
		Short string `yaml:"short"`
	} `yaml:"status"`
	Upgrade struct {
		Short string `yaml:"short"`
		Long  string `yaml:"long"`
	} `yaml:"upgrade"`
	Claude struct {
		Short string `yaml:"short"`
	} `yaml:"claude"`
	ClaudeDownload struct {
		Short string `yaml:"short"`
	} `yaml:"claude_download"`
	ClaudeEnv struct {
		Short string `yaml:"short"`
	} `yaml:"claude_env"`
}

// PromptMessages 对应所有 survey 交互提示的 Message 字段。
type PromptMessages struct {
	// Claude setup
	ClaudeNotFound string `yaml:"claude_not_found"`
	ClaudePath     string `yaml:"claude_path"`
	ClaudeTimeout  string `yaml:"claude_timeout"`
	// Platform
	PlatformSelect      string `yaml:"platform_select"`
	FeishuAppID         string `yaml:"feishu_app_id"`
	FeishuAppSecret     string `yaml:"feishu_app_secret"`
	DingtalkClientID    string `yaml:"dingtalk_client_id"`
	DingtalkClientSecret string `yaml:"dingtalk_client_secret"`
	WecomBotID          string `yaml:"wecom_bot_id"`
	WecomSecret         string `yaml:"wecom_secret"`
	TelegramToken       string `yaml:"telegram_token"`
	TelegramAuthCode    string `yaml:"telegram_auth_code"`
	WeixinReacquire     string `yaml:"weixin_reacquire"` // contains %s placeholder
	// Auth
	Username             string `yaml:"username"`
	PasswordChangeConfirm string `yaml:"password_change_confirm"`
	PasswordSetup        string `yaml:"password_setup"`
	Password             string `yaml:"password"`
	JWTRegenConfirm      string `yaml:"jwt_regen_confirm"`
	// Advanced
	AdvancedConfirm      string `yaml:"advanced_confirm"`
	ServerPort           string `yaml:"server_port"`
	ServerHost           string `yaml:"server_host"`
	DebugMode            string `yaml:"debug_mode"`
	DBPath               string `yaml:"db_path"`
	MCPAPIKeySetup       string `yaml:"mcp_api_key_setup"`
	MCPAPIKey            string `yaml:"mcp_api_key"`
	MCPWorkerAPIKeySetup string `yaml:"mcp_worker_api_key_setup"`
	MCPWorkerAPIKey      string `yaml:"mcp_worker_api_key"`
	FeederTimeout        string `yaml:"feeder_timeout"`
	MaxConcurrentBee     string `yaml:"max_concurrent_bee"`
	MessageDebounce      string `yaml:"message_debounce"`
	FFprobePath          string `yaml:"ffprobe_path"`
	FFmpegPath           string `yaml:"ffmpeg_path"`
	// Write
	ConfirmWrite string `yaml:"confirm_write"`
}
```

- [ ] **Step 4: 新建 `internal/i18n/i18n.go`**

```go
package i18n

import (
	"embed"
	"fmt"

	"gopkg.in/yaml.v3"
)

//go:embed locales/*.yaml
var localesFS embed.FS

// M 是全局翻译实例，在 main() 最早阶段通过 Load() 初始化，之后只读。
var M *Messages

// SupportedLangs 列出所有支持的语言代码。
var SupportedLangs = []string{"zh", "en"}

// Load 加载指定语言的翻译文件并设置 M。
// 若语言不支持（文件不存在），静默 fallback 到 zh。
func Load(lang string) error {
	data, err := localesFS.ReadFile("locales/" + lang + ".yaml")
	if err != nil {
		data, err = localesFS.ReadFile("locales/zh.yaml")
		if err != nil {
			return fmt.Errorf("i18n: failed to load default locale: %w", err)
		}
	}
	m := &Messages{}
	if err := yaml.Unmarshal(data, m); err != nil {
		return fmt.Errorf("i18n: parse locale %q: %w", lang, err)
	}
	M = m
	return nil
}
```

- [ ] **Step 5: 新建 `internal/i18n/locales/en.yaml`**

```yaml
cmd:
  root:
    short: "OpenBee core service"
  config:
    short: "Interactively generate a config file"
  server:
    short: "Start the OpenBee server"
  stop:
    short: "Stop the running OpenBee daemon"
  restart:
    short: "Restart the OpenBee daemon"
  status:
    short: "Show the status of the OpenBee daemon"
  upgrade:
    short: "Upgrade openbee to the latest version"
    long: "Check for a new version and replace the current binary if one is available."
  claude:
    short: "Manage Claude Code installation and provider configuration"
  claude_download:
    short: "Download Claude Code binary to ~/.openbee/bin/claude"
  claude_env:
    short: "Configure Claude Code provider and environment settings"
prompt:
  claude_not_found: "Claude not found, how would you like to get it?"
  claude_path: "Claude executable path:"
  claude_timeout: "Claude timeout:"
  platform_select: "Which platforms to enable?"
  feishu_app_id: "Feishu App ID:"
  feishu_app_secret: "Feishu App Secret:"
  dingtalk_client_id: "DingTalk Client ID:"
  dingtalk_client_secret: "DingTalk Client Secret:"
  wecom_bot_id: "WeCom Bot ID:"
  wecom_secret: "WeCom Secret:"
  telegram_token: "Telegram Bot Token:"
  telegram_auth_code: "Telegram Auth Code (empty to disable auth):"
  weixin_reacquire: "Existing Weixin token found (%s). Re-acquire via QR code?"
  username: "Username:"
  password_change_confirm: "Password already configured. Change it?"
  password_setup: "Password setup:"
  password: "Password:"
  jwt_regen_confirm: "JWT secret already exists. Regenerate?"
  advanced_confirm: "Customize advanced settings?"
  server_port: "Server port:"
  server_host: "Server Host:"
  debug_mode: "Debug mode?"
  db_path: "Database path:"
  mcp_api_key_setup: "MCP API Key setup:"
  mcp_api_key: "MCP API Key:"
  mcp_worker_api_key_setup: "MCP Worker API Key setup:"
  mcp_worker_api_key: "MCP Worker API Key:"
  feeder_timeout: "Feeder timeout:"
  max_concurrent_bee: "Max concurrent bee processes:"
  message_debounce: "Message debounce:"
  ffprobe_path: "FFprobe path:"
  ffmpeg_path: "FFmpeg path:"
  confirm_write: "Confirm write config file?"
```

- [ ] **Step 6: 新建 `internal/i18n/locales/zh.yaml`**

```yaml
cmd:
  root:
    short: "OpenBee 核心服务"
  config:
    short: "交互式生成配置文件"
  server:
    short: "启动 OpenBee 服务器"
  stop:
    short: "停止运行中的 OpenBee 守护进程"
  restart:
    short: "重启 OpenBee 守护进程"
  status:
    short: "查看 OpenBee 守护进程状态"
  upgrade:
    short: "升级 openbee 至最新版本"
    long: "检查是否有新版本，如有则替换当前二进制文件。"
  claude:
    short: "管理 Claude Code 安装及 AI 提供商配置"
  claude_download:
    short: "下载 Claude Code 二进制文件至 ~/.openbee/bin/claude"
  claude_env:
    short: "配置 Claude Code 提供商及环境设置"
prompt:
  claude_not_found: "未找到 Claude，请选择获取方式："
  claude_path: "Claude 可执行文件路径："
  claude_timeout: "Claude 超时时间："
  platform_select: "选择要启用的平台："
  feishu_app_id: "飞书 App ID："
  feishu_app_secret: "飞书 App Secret："
  dingtalk_client_id: "钉钉 Client ID："
  dingtalk_client_secret: "钉钉 Client Secret："
  wecom_bot_id: "企业微信 Bot ID："
  wecom_secret: "企业微信 Secret："
  telegram_token: "Telegram Bot Token："
  telegram_auth_code: "Telegram 授权码（留空则不限制用户）："
  weixin_reacquire: "已有微信 token（%s）。是否重新扫码登录？"
  username: "用户名："
  password_change_confirm: "已配置密码，是否修改？"
  password_setup: "密码设置方式："
  password: "密码："
  jwt_regen_confirm: "JWT 密钥已存在，是否重新生成？"
  advanced_confirm: "是否自定义高级设置？"
  server_port: "服务端口："
  server_host: "服务地址："
  debug_mode: "开启调试模式？"
  db_path: "数据库路径："
  mcp_api_key_setup: "MCP API Key 设置方式："
  mcp_api_key: "MCP API Key："
  mcp_worker_api_key_setup: "MCP Worker API Key 设置方式："
  mcp_worker_api_key: "MCP Worker API Key："
  feeder_timeout: "Feeder 超时时间："
  max_concurrent_bee: "最大并发 bee 数量："
  message_debounce: "消息防抖时间："
  ffprobe_path: "FFprobe 路径："
  ffmpeg_path: "FFmpeg 路径："
  confirm_write: "确认写入配置文件？"
```

- [ ] **Step 7: 运行测试，确认通过**

```bash
go test ./internal/i18n/... -v
```

预期：`PASS`，3 个测试全部通过

- [ ] **Step 8: Commit**

```bash
git add internal/i18n/
git commit -m "feat(i18n): add core i18n package with zh/en YAML locales"
```

---

## Task 2: config 包 — 新增 Language 字段和 GetLang()

**Files:**
- Modify: `internal/config/config.go`

- [ ] **Step 1: 写失败测试**

在 `internal/config/config_bee_test.go` 末尾追加：

```go
func TestGetLang_fromFile(t *testing.T) {
	f, _ := os.CreateTemp("", "*.yaml")
	f.WriteString(`language: en`)
	f.Close()
	defer os.Remove(f.Name())

	if got := GetLang(f.Name()); got != "en" {
		t.Errorf("GetLang: got %q, want %q", got, "en")
	}
}

func TestGetLang_missing(t *testing.T) {
	if got := GetLang("/nonexistent/path.yaml"); got != "" {
		t.Errorf("GetLang with missing file: got %q, want empty", got)
	}
}

func TestGetLang_notSet(t *testing.T) {
	f, _ := os.CreateTemp("", "*.yaml")
	f.WriteString(`server:
  port: 8080`)
	f.Close()
	defer os.Remove(f.Name())

	if got := GetLang(f.Name()); got != "" {
		t.Errorf("GetLang without language field: got %q, want empty", got)
	}
}
```

- [ ] **Step 2: 运行测试，确认失败（GetLang 未定义）**

```bash
go test ./internal/config/... -run TestGetLang -v
```

预期：`undefined: GetLang`

- [ ] **Step 3: 在 `internal/config/config.go` 的 `Config` struct 中新增 Language 字段**

找到：
```go
type Config struct {
	Server   ServerConfig   `yaml:"server"`
	Database DatabaseConfig `yaml:"database"`
	Bee      BeeConfig      `yaml:"bee"`
}
```

改为：
```go
type Config struct {
	Language string         `yaml:"language"`
	Server   ServerConfig   `yaml:"server"`
	Database DatabaseConfig `yaml:"database"`
	Bee      BeeConfig      `yaml:"bee"`
}
```

- [ ] **Step 4: 在 `internal/config/config.go` 末尾添加 `GetLang()` 函数**

在文件末尾（`applyDefaults` 函数之后）追加：

```go
// GetLang reads the language field from the config file at path.
// Returns empty string if the file does not exist, cannot be parsed,
// or does not contain a language field. Never returns an error —
// callers fall back to the next priority in the detection chain.
func GetLang(path string) string {
	data, err := os.ReadFile(path)
	if err != nil {
		return ""
	}
	var cfg struct {
		Language string `yaml:"language"`
	}
	if err := yaml.Unmarshal(data, &cfg); err != nil {
		return ""
	}
	return cfg.Language
}
```

- [ ] **Step 5: 运行测试，确认通过**

```bash
go test ./internal/config/... -run TestGetLang -v
```

预期：`PASS`，3 个测试全部通过

- [ ] **Step 6: Commit**

```bash
git add internal/config/config.go internal/config/config_bee_test.go
git commit -m "feat(config): add Language field and GetLang() helper"
```

---

## Task 3: CLI 语言检测 — lang.go + main.go 改造

**Files:**
- Create: `cmd/openbee/lang.go`
- Modify: `cmd/openbee/main.go`

- [ ] **Step 1: 新建 `cmd/openbee/lang.go`**

```go
package main

import (
	"os"
	"strings"

	"github.com/theopenbee/openbee/internal/config"
)

// parseLangFlag scans os.Args for --lang <value> or --lang=<value>
// without triggering cobra parsing. Returns empty string if not found.
func parseLangFlag(args []string) string {
	for i, arg := range args {
		if arg == "--lang" && i+1 < len(args) {
			return args[i+1]
		}
		if strings.HasPrefix(arg, "--lang=") {
			return strings.TrimPrefix(arg, "--lang=")
		}
	}
	return ""
}

// detectLang determines the UI language using priority:
// CLI flag > OPENBEE_LANG env > config.yaml language field > default "zh".
// flagLang is the value extracted by parseLangFlag (may be empty).
func detectLang(flagLang string) string {
	if flagLang != "" {
		return flagLang
	}
	if env := os.Getenv("OPENBEE_LANG"); env != "" {
		return env
	}
	// Best-effort: try the default config path.
	if lang := config.GetLang("config.yaml"); lang != "" {
		return lang
	}
	return "zh"
}
```

- [ ] **Step 2: 新建 `cmd/openbee/lang_test.go`** (放在同一包下，用 `package main`)

```go
package main

import (
	"os"
	"testing"
)

func TestParseLangFlag(t *testing.T) {
	tests := []struct {
		args []string
		want string
	}{
		{[]string{"openbee", "--lang", "en", "config"}, "en"},
		{[]string{"openbee", "--lang=zh", "server"}, "zh"},
		{[]string{"openbee", "server"}, ""},
		{[]string{}, ""},
	}
	for _, tt := range tests {
		if got := parseLangFlag(tt.args); got != tt.want {
			t.Errorf("parseLangFlag(%v) = %q, want %q", tt.args, got, tt.want)
		}
	}
}

func TestDetectLang_flag(t *testing.T) {
	os.Unsetenv("OPENBEE_LANG")
	if got := detectLang("en"); got != "en" {
		t.Errorf("detectLang flag: got %q, want en", got)
	}
}

func TestDetectLang_env(t *testing.T) {
	os.Setenv("OPENBEE_LANG", "en")
	defer os.Unsetenv("OPENBEE_LANG")
	if got := detectLang(""); got != "en" {
		t.Errorf("detectLang env: got %q, want en", got)
	}
}

func TestDetectLang_default(t *testing.T) {
	os.Unsetenv("OPENBEE_LANG")
	if got := detectLang(""); got != "zh" {
		t.Errorf("detectLang default: got %q, want zh", got)
	}
}
```

- [ ] **Step 3: 运行测试，确认通过**

```bash
go test ./cmd/openbee/... -run "TestParseLangFlag|TestDetectLang" -v
```

预期：`PASS`，4 个测试全部通过

- [ ] **Step 4: 改造 `cmd/openbee/main.go`**

在文件顶部 import 中新增：
```go
import (
    "errors"
    "fmt"
    "os"
    "path/filepath"

    "github.com/spf13/cobra"
    "go.uber.org/zap"

    "github.com/theopenbee/openbee/internal/i18n"
    "github.com/theopenbee/openbee/internal/logger"
)
```

在 `init()` 函数中（`rootCmd.SetVersionTemplate` 之后）添加 `--lang` persistent flag：

```go
func init() {
	rootCmd.SetVersionTemplate(fmt.Sprintf("openbee %s (commit: %s, built: %s)\n", version, commit, date))
	rootCmd.PersistentFlags().String("lang", "", "UI language (zh, en). Overrides OPENBEE_LANG and config.yaml language field")
}
```

在 `main()` 函数中，在 `rootCmd.Execute()` 之前插入语言加载逻辑：

找到：
```go
func main() {
	if err := rootCmd.Execute(); err != nil {
```

改为：
```go
func main() {
	// Detect and load language before Execute() so cobra Short/Long fields
	// (set in init()) can be overridden by applyTranslations().
	lang := detectLang(parseLangFlag(os.Args))
	if err := i18n.Load(lang); err != nil {
		fmt.Fprintf(os.Stderr, "warning: i18n load failed: %v\n", err)
	}
	applyTranslations()

	if err := rootCmd.Execute(); err != nil {
```

在 `main.go` 末尾（`resolveExecutable` 函数之后）新增 `applyTranslations()`：

```go
// applyTranslations overwrites cobra command Short/Long fields with the
// loaded locale. Must be called after i18n.Load() and before Execute().
func applyTranslations() {
	m := i18n.M
	rootCmd.Short = m.Cmd.Root.Short
	configCmd.Short = m.Cmd.Config.Short
	serverCmd.Short = m.Cmd.Server.Short
	stopCmd.Short = m.Cmd.Stop.Short
	restartCmd.Short = m.Cmd.Restart.Short
	statusCmd.Short = m.Cmd.Status.Short
	upgradeCmd.Short = m.Cmd.Upgrade.Short
	upgradeCmd.Long = m.Cmd.Upgrade.Long
	claudeCmd.Short = m.Cmd.Claude.Short
	claudeDownloadCmd.Short = m.Cmd.ClaudeDownload.Short
	claudeEnvCmd.Short = m.Cmd.ClaudeEnv.Short
}
```

- [ ] **Step 5: 确认编译通过**

```bash
go build ./cmd/openbee/...
```

预期：无错误

- [ ] **Step 6: 验证 --help 输出随语言切换**

```bash
# 默认（zh）
./openbee --help | head -5

# 英文
OPENBEE_LANG=en ./openbee --help | head -5
```

预期：第一次输出 "OpenBee 核心服务"，第二次输出 "OpenBee core service"

- [ ] **Step 7: Commit**

```bash
git add cmd/openbee/lang.go cmd/openbee/lang_test.go cmd/openbee/main.go
git commit -m "feat(i18n): add lang detection and cobra command translation wiring"
```

---

## Task 4: 替换 config.go 中的 survey 提示文字

**Files:**
- Modify: `cmd/openbee/config.go`

- [ ] **Step 1: 在 `cmd/openbee/config.go` 顶部 import 中添加 i18n 包**

找到：
```go
import (
	"bytes"
	"crypto/rand"
	"encoding/hex"
	"errors"
	"fmt"
	"os"
	"strconv"
	"text/template"

	"github.com/AlecAivazis/survey/v2"
	"github.com/spf13/cobra"
	"github.com/theopenbee/openbee/internal/claude"
	"github.com/theopenbee/openbee/internal/config"
)
```

改为：
```go
import (
	"bytes"
	"crypto/rand"
	"encoding/hex"
	"errors"
	"fmt"
	"os"
	"strconv"
	"text/template"

	"github.com/AlecAivazis/survey/v2"
	"github.com/spf13/cobra"
	"github.com/theopenbee/openbee/internal/claude"
	"github.com/theopenbee/openbee/internal/config"
	"github.com/theopenbee/openbee/internal/i18n"
)
```

- [ ] **Step 2: 替换 `runConfig()` 中所有 survey `Message` 字段**

将 `runConfig()` 函数中所有 survey 调用的 `Message` 硬编码字符串替换为 `i18n.M.Prompt.*`：

| 原始字符串 | 替换为 |
|---|---|
| `"Which platforms to enable?"` | `i18n.M.Prompt.PlatformSelect` |
| `"Feishu App ID:"` | `i18n.M.Prompt.FeishuAppID` |
| `"Feishu App Secret:"` | `i18n.M.Prompt.FeishuAppSecret` |
| `"DingTalk Client ID:"` | `i18n.M.Prompt.DingtalkClientID` |
| `"DingTalk Client Secret:"` | `i18n.M.Prompt.DingtalkClientSecret` |
| `"WeCom Bot ID:"` | `i18n.M.Prompt.WecomBotID` |
| `"WeCom Secret:"` | `i18n.M.Prompt.WecomSecret` |
| `"Telegram Bot Token:"` | `i18n.M.Prompt.TelegramToken` |
| `"Telegram Auth Code (empty to disable auth):"` | `i18n.M.Prompt.TelegramAuthCode` |
| `fmt.Sprintf("Existing Weixin token found (%s). Re-acquire via QR code?", masked)` | `fmt.Sprintf(i18n.M.Prompt.WeixinReacquire, masked)` |
| `"Username:"` | `i18n.M.Prompt.Username` |
| `"Password already configured. Change it?"` | `i18n.M.Prompt.PasswordChangeConfirm` |
| `"JWT secret already exists. Regenerate?"` | `i18n.M.Prompt.JWTRegenConfirm` |
| `"Customize advanced settings?"` | `i18n.M.Prompt.AdvancedConfirm` |
| `"Server port:"` | `i18n.M.Prompt.ServerPort` |
| `"Server Host:"` | `i18n.M.Prompt.ServerHost` |
| `"Debug mode?"` | `i18n.M.Prompt.DebugMode` |
| `"Database path:"` | `i18n.M.Prompt.DBPath` |
| `"MCP API Key setup:"` | `i18n.M.Prompt.MCPAPIKeySetup` |
| `"MCP API Key:"` | `i18n.M.Prompt.MCPAPIKey` |
| `"MCP Worker API Key setup:"` | `i18n.M.Prompt.MCPWorkerAPIKeySetup` |
| `"MCP Worker API Key:"` | `i18n.M.Prompt.MCPWorkerAPIKey` |
| `"Feeder timeout:"` | `i18n.M.Prompt.FeederTimeout` |
| `"Max concurrent bee processes:"` | `i18n.M.Prompt.MaxConcurrentBee` |
| `"Message debounce:"` | `i18n.M.Prompt.MessageDebounce` |
| `"FFprobe path:"` | `i18n.M.Prompt.FFprobePath` |
| `"FFmpeg path:"` | `i18n.M.Prompt.FFmpegPath` |
| `"Confirm write config file?"` | `i18n.M.Prompt.ConfirmWrite` |

替换 `promptPassword()` 中：

| 原始字符串 | 替换为 |
|---|---|
| `"Password setup:"` | `i18n.M.Prompt.PasswordSetup` |
| `"Password:"` | `i18n.M.Prompt.Password` |

- [ ] **Step 3: 确认编译通过**

```bash
go build ./cmd/openbee/...
```

预期：无错误

- [ ] **Step 4: Commit**

```bash
git add cmd/openbee/config.go
git commit -m "feat(i18n): wire survey prompts in config.go to i18n.M.Prompt"
```

---

## Task 5: 替换 config_claude.go 中的 survey 提示文字

**Files:**
- Modify: `cmd/openbee/config_claude.go`

- [ ] **Step 1: 在 `cmd/openbee/config_claude.go` import 中添加 i18n 包**

找到：
```go
import (
	"fmt"
	"os"
	"os/exec"

	"github.com/AlecAivazis/survey/v2"
	claude "github.com/theopenbee/openbee/internal/claude"
)
```

改为：
```go
import (
	"fmt"
	"os"
	"os/exec"

	"github.com/AlecAivazis/survey/v2"
	claude "github.com/theopenbee/openbee/internal/claude"
	"github.com/theopenbee/openbee/internal/i18n"
)
```

- [ ] **Step 2: 替换 `configureClaudeExecutable()` 中的 survey `Message` 字段**

| 原始字符串 | 替换为 |
|---|---|
| `"Claude not found, how would you like to get it?"` | `i18n.M.Prompt.ClaudeNotFound` |
| `"Claude executable path:"` | `i18n.M.Prompt.ClaudePath` |
| `"Claude timeout:"` | `i18n.M.Prompt.ClaudeTimeout` |

- [ ] **Step 3: 确认编译通过**

```bash
go build ./cmd/openbee/...
```

预期：无错误

- [ ] **Step 4: Commit**

```bash
git add cmd/openbee/config_claude.go
git commit -m "feat(i18n): wire survey prompts in config_claude.go to i18n.M.Prompt"
```

---

## Task 6: 端到端验证

- [ ] **Step 1: 构建二进制**

```bash
go build -o /tmp/openbee-test ./cmd/openbee/
```

预期：无错误

- [ ] **Step 2: 验证默认语言（zh）**

```bash
/tmp/openbee-test --help
```

预期输出包含：
```
OpenBee 核心服务
```

- [ ] **Step 3: 验证环境变量切换（en）**

```bash
OPENBEE_LANG=en /tmp/openbee-test --help
```

预期输出包含：
```
OpenBee core service
```

- [ ] **Step 4: 验证 --lang flag**

```bash
/tmp/openbee-test --lang en --help
```

预期输出包含：
```
OpenBee core service
```

- [ ] **Step 5: 验证不支持的语言 fallback**

```bash
OPENBEE_LANG=fr /tmp/openbee-test --help
```

预期：不报错，输出中文（fallback 到 zh）

- [ ] **Step 6: 运行所有测试**

```bash
go test ./internal/i18n/... ./internal/config/... ./cmd/openbee/... -v 2>&1 | tail -20
```

预期：所有测试 `PASS`，无 `FAIL`

- [ ] **Step 7: 清理临时二进制并做最终 commit**

```bash
rm /tmp/openbee-test
git log --oneline -6
```

预期：看到 Task 1~5 的 5 个 commit
