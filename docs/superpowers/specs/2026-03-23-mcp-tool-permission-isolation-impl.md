# MCP 工具权限隔离 — 详细实现计划

> 日期：2026-03-23
> 关联文档：
> - 设计文档：`2026-03-23-mcp-tool-permission-isolation-design.md`
> - 实施计划：`2026-03-23-mcp-tool-permission-isolation-plan.md`
> 状态：草案

---

## 概述

本文档是面向开发人员的逐步实现指南，包含每个步骤需要修改的精确代码、新增的数据结构、接口变更和测试用例。开发人员可以按照步骤顺序直接编码实现。

**总体原则**：每完成一个步骤后应确保 `go build ./...` 通过。每完成一个阶段后应确保 `go test ./...` 通过。

---

## 步骤 1：配置层 — MCPConfig 新增 WorkerAPIKey

### 1.1 修改 `internal/config/config.go`

**当前代码**（第 111-113 行）：

```go
type MCPConfig struct {
	APIKey string `yaml:"api_key"`
}
```

**目标代码**：

```go
type MCPConfig struct {
	APIKey       string `yaml:"api_key"`
	WorkerAPIKey string `yaml:"worker_api_key"`
}
```

**在 `applyDefaults()` 函数末尾新增**（约第 197 行之前）：

```go
// 自动生成 Worker API Key
if cfg.Bee.MCP.WorkerAPIKey == "" && cfg.Bee.MCP.APIKey != "" {
	cfg.Bee.MCP.WorkerAPIKey = generateRandomKey()
}
```

**新增辅助函数**（文件末尾）：

```go
import "crypto/rand"

func generateRandomKey() string {
	b := make([]byte, 32)
	if _, err := rand.Read(b); err != nil {
		panic("crypto/rand failed: " + err.Error())
	}
	return fmt.Sprintf("%x", b)
}
```

**输入**：YAML 配置文件中可能有也可能没有 `worker_api_key`
**输出**：`cfg.Bee.MCP.WorkerAPIKey` 始终非空（要么来自配置，要么自动生成）

### 1.2 修改 `internal/config/config.yaml.tmpl`

**当前代码**（第 22-23 行）：

```yaml
  mcp:
    api_key: "{{.MCPAPIKey}}"
```

**目标代码**：

```yaml
  mcp:
    api_key: "{{.MCPAPIKey}}"
    worker_api_key: ""  # Worker 专用 API Key；留空则启动时自动生成
```

### 1.3 修改 `internal/app/app.go`

**当前代码**（第 92-94 行）：

```go
if cfg.Bee.MCP.APIKey == "" {
	return nil, fmt.Errorf("bee.mcp.api_key must be set — bee requires MCP to create tasks")
}
```

**目标代码**：

```go
if cfg.Bee.MCP.APIKey == "" {
	return nil, fmt.Errorf("bee.mcp.api_key must be set — bee requires MCP to create tasks")
}
if cfg.Bee.MCP.WorkerAPIKey == "" {
	return nil, fmt.Errorf("bee.mcp.worker_api_key must be set (should be auto-generated by applyDefaults)")
}
```

### 1.4 新增测试 `internal/config/config_bee_test.go`

在文件末尾追加：

```go
func TestBeeConfig_WorkerAPIKeyAutoGenerated(t *testing.T) {
	f, _ := os.CreateTemp("", "*.yaml")
	f.WriteString(`
server:
  port: 8080
bee:
  mcp:
    api_key: "test-key"
`)
	f.Close()

	cfg, err := Load(f.Name())
	if err != nil {
		t.Fatalf("Load: %v", err)
	}
	if cfg.Bee.MCP.WorkerAPIKey == "" {
		t.Error("WorkerAPIKey should be auto-generated when not set")
	}
	if cfg.Bee.MCP.WorkerAPIKey == cfg.Bee.MCP.APIKey {
		t.Error("WorkerAPIKey should differ from APIKey")
	}
}

func TestBeeConfig_WorkerAPIKeyExplicit(t *testing.T) {
	f, _ := os.CreateTemp("", "*.yaml")
	f.WriteString(`
server:
  port: 8080
bee:
  mcp:
    api_key: "bee-key"
    worker_api_key: "worker-key"
`)
	f.Close()

	cfg, err := Load(f.Name())
	if err != nil {
		t.Fatalf("Load: %v", err)
	}
	if cfg.Bee.MCP.WorkerAPIKey != "worker-key" {
		t.Errorf("WorkerAPIKey: want worker-key got %q", cfg.Bee.MCP.WorkerAPIKey)
	}
}
```

### 步骤 1 验证

```bash
go build ./...
go test ./internal/config/... -v -run TestBeeConfig_Worker
```

---

## 步骤 2：Invoker URL 拼接调整

### 2.1 修改 `internal/claude/invoker.go`

**当前代码**（第 44-49 行）：

```go
// NewInvoker creates an Invoker. mcpBaseURL is joined with "/mcp/sse".
func NewInvoker(binary, mcpBaseURL, apiKey string) *Invoker {
	return &Invoker{
		binary: binary,
		mcpURL: mcpBaseURL + "/mcp/sse",
		apiKey: apiKey,
	}
}
```

**目标代码**：

```go
// NewInvoker creates an Invoker. mcpBasePath should include the full MCP base
// path (e.g. "http://host:port/mcp/bee"); "/sse" is appended automatically.
func NewInvoker(binary, mcpBasePath, apiKey string) *Invoker {
	return &Invoker{
		binary: binary,
		mcpURL: mcpBasePath + "/sse",
		apiKey: apiKey,
	}
}
```

**输入变更**：
- 之前：`mcpBaseURL = "http://localhost:8080"`，内部拼接 `"/mcp/sse"` → `http://localhost:8080/mcp/sse`
- 之后：`mcpBasePath = "http://localhost:8080/mcp/bee"`，内部拼接 `"/sse"` → `http://localhost:8080/mcp/bee/sse`

### 2.2 更新 `internal/claude/invoker_test.go`

**当前代码**（第 11-22 行）：

```go
func TestNewInvoker(t *testing.T) {
	inv := NewInvoker("/usr/bin/claude", "http://localhost:8080", "test-key")
	// ...
	if inv.mcpURL != "http://localhost:8080/mcp/sse" {
```

**目标代码**：

```go
func TestNewInvoker(t *testing.T) {
	inv := NewInvoker("/usr/bin/claude", "http://localhost:8080/mcp/bee", "test-key")
	// ...
	if inv.mcpURL != "http://localhost:8080/mcp/bee/sse" {
```

### 步骤 2 验证

```bash
go test ./internal/claude/... -v -run TestNewInvoker
```

---

## 步骤 3：MCP Server 工具分组

### 3.1 修改 `internal/mcp/tools.go` — 工具定义分组

**当前代码**（第 30 行）：

```go
func toolSchemas() []toolSchema {
	return []toolSchema{
		// ... 全部 19 个工具定义 ...
	}
}
```

**目标代码**：

将 `toolSchemas()` 重命名为 `beeToolSchemas()`，保持返回全部 19 个工具。

新增 `workerToolSchemas()` 函数，仅包含 5 个 Worker 工具的定义。从 `beeToolSchemas` 中提取以下工具的 schema 定义（直接复制或引用）：

```go
// workerToolSchemas 返回 Worker 可用的工具定义（5 个）
func workerToolSchemas() []toolSchema {
	return []toolSchema{
		// 从 beeToolSchemas 中复制以下 5 个工具的完整 toolSchema：
		// 1. toolnames.MarkTaskComplete
		// 2. toolnames.SendMessage
		// 3. toolnames.SaveMemory
		// 4. toolnames.GetMemory
		// 5. toolnames.DeleteMemory
	}
}
```

更新导出函数：

```go
// ToolSchemas (exported) 返回 Bee 工具集，保持向后兼容
func ToolSchemas() []toolSchema {
	return beeToolSchemas()
}
```

### 3.2 修改 `internal/mcp/tools.go` — 工具调用分组

**当前代码**（第 266-309 行）：

```go
func (s *MCPServer) callTool(name string, args json.RawMessage) (any, error) {
	switch name {
	case toolnames.ListWorkers:
		return s.toolListWorkers(args)
	// ... 全部 19 个 case ...
	default:
		return nil, fmt.Errorf("unknown tool: %s", name)
	}
}
```

**目标代码**：

将 `callTool` 重命名为 `beeCallTool`，保持全部 19 个 case 不变。

新增 `workerCallTool`：

```go
// workerCallTool 仅分发 Worker 可用的 5 个工具
func (s *MCPServer) workerCallTool(name string, args json.RawMessage) (any, error) {
	switch name {
	case toolnames.MarkTaskComplete:
		return s.toolMarkTaskComplete(args)
	case toolnames.SendMessage:
		return s.toolSendMessage(args)
	case toolnames.SaveMemory:
		return s.toolSaveMemory(args)
	case toolnames.GetMemory:
		return s.toolGetMemory(args)
	case toolnames.DeleteMemory:
		return s.toolDeleteMemory(args)
	default:
		return nil, fmt.Errorf("unknown tool: %s", name)
	}
}
```

更新导出的 `CallTool` 方法（用于测试）：

```go
// CallTool is exported for testing.
func (s *MCPServer) CallTool(name string, args json.RawMessage) (any, error) {
	return s.callToolFn(name, args)
}
```

### 步骤 3 验证

此步骤结束后 `callToolFn` 尚未赋值，需配合步骤 4 使用。先确保编译通过。

---

## 步骤 4：MCPServer 结构体与构造函数拆分

### 4.1 修改 `internal/mcp/server.go` — 结构体变更

**当前 MCPServer 结构体**（第 66-80 行）：

```go
type MCPServer struct {
	workerStore    *store.WorkerStore
	manager        *worker.Manager
	taskStore      *store.TaskStore
	messageStore   *store.MessageStore
	senders        map[string]platform.PlatformSenderAdapter
	execStopper    ExecutionStopper
	sessionClearer SessionClearer
	executionStore *store.ExecutionStore
	memoryStore    *store.MemoryStore
	sessionStore   *store.SessionStore

	mu       sync.Mutex
	sessions map[string]chan rpcResponse
}
```

**目标 MCPServer 结构体**：

```go
type MCPServer struct {
	// Tool dispatch — set by constructor
	schemasFn   func() []toolSchema
	callToolFn  func(name string, args json.RawMessage) (any, error)

	// Dependencies — not all fields are used by all server variants.
	// Worker server leaves unused fields at zero value.
	workerStore    *store.WorkerStore
	manager        *worker.Manager
	taskStore      *store.TaskStore
	messageStore   *store.MessageStore
	senders        map[string]platform.PlatformSenderAdapter
	execStopper    ExecutionStopper
	sessionClearer SessionClearer
	executionStore *store.ExecutionStore
	memoryStore    *store.MemoryStore
	sessionStore   *store.SessionStore

	mu       sync.Mutex
	sessions map[string]chan rpcResponse
}
```

### 4.2 修改 `internal/mcp/server.go` — dispatch 方法变更

**当前 dispatch**（第 224-225 行）：

```go
case "tools/list":
	return okResponse(req.ID, map[string]any{"tools": toolSchemas()})
```

**目标代码**：

```go
case "tools/list":
	return okResponse(req.ID, map[string]any{"tools": s.schemasFn()})
```

**当前 handleToolCall**（第 245 行）：

```go
result, err := s.callTool(params.Name, params.Arguments)
```

**目标代码**：

```go
result, err := s.callToolFn(params.Name, params.Arguments)
```

### 4.3 新增构造函数

**保留现有 `NewServer` 作为 `NewBeeServer` 的别名**，避免破坏现有调用方和测试：

```go
// NewBeeServer creates a Bee MCP Server with access to all 19 tools.
func NewBeeServer(
	ws *store.WorkerStore,
	mgr *worker.Manager,
	ts *store.TaskStore,
	ms *store.MessageStore,
	senders map[string]platform.PlatformSenderAdapter,
	execStopper ExecutionStopper,
	sessionClearer SessionClearer,
	es *store.ExecutionStore,
	memStore *store.MemoryStore,
	sessionStore *store.SessionStore,
) *MCPServer {
	s := &MCPServer{
		workerStore:    ws,
		manager:        mgr,
		taskStore:      ts,
		messageStore:   ms,
		senders:        senders,
		execStopper:    execStopper,
		sessionClearer: sessionClearer,
		executionStore: es,
		memoryStore:    memStore,
		sessionStore:   sessionStore,
		sessions:       make(map[string]chan rpcResponse),
	}
	s.schemasFn = beeToolSchemas
	s.callToolFn = s.beeCallTool
	return s
}

// NewWorkerServer creates a Worker MCP Server with access to only 5 tools:
// send_message, mark_task_complete, save_memory, get_memory, delete_memory.
func NewWorkerServer(
	ts *store.TaskStore,
	ms *store.MessageStore,
	senders map[string]platform.PlatformSenderAdapter,
	memStore *store.MemoryStore,
) *MCPServer {
	s := &MCPServer{
		taskStore:    ts,
		messageStore: ms,
		senders:      senders,
		memoryStore:  memStore,
		sessions:     make(map[string]chan rpcResponse),
	}
	s.schemasFn = workerToolSchemas
	s.callToolFn = s.workerCallTool
	return s
}

// NewServer creates a Bee MCP Server (backward-compatible alias for NewBeeServer).
func NewServer(
	ws *store.WorkerStore,
	mgr *worker.Manager,
	ts *store.TaskStore,
	ms *store.MessageStore,
	senders map[string]platform.PlatformSenderAdapter,
	execStopper ExecutionStopper,
	sessionClearer SessionClearer,
	es *store.ExecutionStore,
	memStore *store.MemoryStore,
	sessionStore *store.SessionStore,
) *MCPServer {
	return NewBeeServer(ws, mgr, ts, ms, senders, execStopper, sessionClearer, es, memStore, sessionStore)
}
```

### 4.4 关键依赖分析

Worker 工具方法对 MCPServer 字段的依赖：

| 工具方法 | 使用的字段 |
|---------|----------|
| `toolMarkTaskComplete` | `taskStore` |
| `toolSendMessage` | `messageStore`, `senders` |
| `toolSaveMemory` | `memoryStore` |
| `toolGetMemory` | `memoryStore` |
| `toolDeleteMemory` | `memoryStore` |

`NewWorkerServer` 注入的字段恰好覆盖以上依赖。Bee 专用工具方法依赖的 `workerStore`、`manager`、`executionStore`、`execStopper`、`sessionClearer`、`sessionStore` 在 Worker Server 中为 nil，但由于 `workerCallTool` 不会路由到这些方法，所以不会触发 nil panic。

### 步骤 4 验证

```bash
go build ./...
go test ./internal/mcp/... -v
```

---

## 步骤 5：路由层双端点注册

### 5.1 修改 `internal/api/router.go` — 结构体变更

**当前 Server 结构体**（第 18-31 行）：

```go
type Server struct {
	router           *gin.Engine
	httpServer       *http.Server
	workerStore      *store.WorkerStore
	executionStore   *store.ExecutionStore
	manager          *worker.Manager
	logRegistry      *worker.ActiveLogRegistry
	mcpServer        *mcp.MCPServer
	mcpAPIKey        string
	staticFS         fs.FS
	localChatHandler *LocalChatHandler
	authHandler      *auth.AuthHandler
	jwtMiddleware    gin.HandlerFunc
}
```

**目标 Server 结构体**：

```go
type Server struct {
	router           *gin.Engine
	httpServer       *http.Server
	workerStore      *store.WorkerStore
	executionStore   *store.ExecutionStore
	manager          *worker.Manager
	logRegistry      *worker.ActiveLogRegistry
	beeMCPServer     *mcp.MCPServer
	workerMCPServer  *mcp.MCPServer
	beeAPIKey        string
	workerAPIKey     string
	staticFS         fs.FS
	localChatHandler *LocalChatHandler
	authHandler      *auth.AuthHandler
	jwtMiddleware    gin.HandlerFunc
}
```

### 5.2 修改 `internal/api/router.go` — NewServer 签名变更

**当前签名**（第 33 行）：

```go
func NewServer(
	ws *store.WorkerStore,
	es *store.ExecutionStore,
	mgr *worker.Manager,
	logRegistry *worker.ActiveLogRegistry,
	mcpSrv *mcp.MCPServer,
	mcpAPIKey string,
	staticFS fs.FS,
	localChat *LocalChatHandler,
	authHandler *auth.AuthHandler,
	jwtMiddleware gin.HandlerFunc,
) (*Server, error) {
```

**目标签名**：

```go
func NewServer(
	ws *store.WorkerStore,
	es *store.ExecutionStore,
	mgr *worker.Manager,
	logRegistry *worker.ActiveLogRegistry,
	beeMCPSrv *mcp.MCPServer,
	workerMCPSrv *mcp.MCPServer,
	beeAPIKey string,
	workerAPIKey string,
	staticFS fs.FS,
	localChat *LocalChatHandler,
	authHandler *auth.AuthHandler,
	jwtMiddleware gin.HandlerFunc,
) (*Server, error) {
```

**构造体内字段赋值对应更新**：

```go
s := &Server{
	// ...existing fields...
	beeMCPServer:     beeMCPSrv,
	workerMCPServer:  workerMCPSrv,
	beeAPIKey:        beeAPIKey,
	workerAPIKey:     workerAPIKey,
	// ...
}
```

### 5.3 修改 `internal/api/router.go` — registerMCPRoutes 变更

**当前代码**（第 119-124 行）：

```go
func (s *Server) registerMCPRoutes() {
	mcpGroup := s.router.Group("/mcp")
	mcpGroup.Use(mcp.APIKeyMiddleware(s.mcpAPIKey))
	mcpGroup.GET("/sse", s.mcpServer.HandleSSE)
	mcpGroup.POST("/messages", s.mcpServer.HandleMessages)
}
```

**目标代码**：

```go
func (s *Server) registerMCPRoutes() {
	// Bee MCP endpoints
	beeGroup := s.router.Group("/mcp/bee")
	beeGroup.Use(mcp.APIKeyMiddleware(s.beeAPIKey))
	beeGroup.GET("/sse", s.beeMCPServer.HandleSSE)
	beeGroup.POST("/messages", s.beeMCPServer.HandleMessages)

	// Worker MCP endpoints
	workerGroup := s.router.Group("/mcp/worker")
	workerGroup.Use(mcp.APIKeyMiddleware(s.workerAPIKey))
	workerGroup.GET("/sse", s.workerMCPServer.HandleSSE)
	workerGroup.POST("/messages", s.workerMCPServer.HandleMessages)

	// Backward-compatible: legacy /mcp/* routes → Bee server
	legacyGroup := s.router.Group("/mcp")
	legacyGroup.Use(mcp.APIKeyMiddleware(s.beeAPIKey))
	legacyGroup.GET("/sse", s.beeMCPServer.HandleSSE)
	legacyGroup.POST("/messages", s.beeMCPServer.HandleMessages)
}
```

### 5.4 修改 gzip 排除列表

**当前代码**（第 46-50 行）：

```go
router.Use(gzip.Gzip(gzip.DefaultCompression, gzip.WithExcludedPathsRegexs([]string{
	"/api/local/sessions/.+/stream",
	"/mcp/sse",
	"/mcp/messages",
})))
```

**目标代码**：

```go
router.Use(gzip.Gzip(gzip.DefaultCompression, gzip.WithExcludedPathsRegexs([]string{
	"/api/local/sessions/.+/stream",
	"/mcp/.*/sse",
	"/mcp/.*/messages",
	"/mcp/sse",
	"/mcp/messages",
})))
```

### 步骤 5 验证

此步骤与步骤 6（Bee/Worker 连接更新）和步骤 7（App 装配）一起验证编译。

---

## 步骤 6：Bee 和 Worker 连接层更新

### 6.1 修改 `internal/bee/bee_process.go`

**当前代码**（第 22-26 行）：

```go
func NewBeeProcess(cfg config.BeeConfig) *BeeProcess {
	return &BeeProcess{
		invoker: claude.NewInvoker(cfg.Claude.Path, cfg.MCPBaseURL, cfg.MCP.APIKey),
	}
}
```

**目标代码**：

```go
func NewBeeProcess(cfg config.BeeConfig) *BeeProcess {
	return &BeeProcess{
		invoker: claude.NewInvoker(cfg.Claude.Path, cfg.MCPBaseURL+"/mcp/bee", cfg.MCP.APIKey),
	}
}
```

### 6.2 修改 `internal/worker/manager.go`

**当前代码**（第 64 行）：

```go
invoker: claude.NewInvoker(bc.Claude.Path, bc.MCPBaseURL, bc.MCP.APIKey),
```

**目标代码**：

```go
invoker: claude.NewInvoker(bc.Claude.Path, bc.MCPBaseURL+"/mcp/worker", bc.MCP.WorkerAPIKey),
```

### 步骤 6 验证

配合步骤 7 一起验证编译。

---

## 步骤 7：App 装配层更新

### 7.1 修改 `internal/app/app.go` — 双 MCP Server 实例

**当前代码**（第 120 行）：

```go
mcpSrv := mcp.NewServer(s.workerStore, mgr, s.taskStore, s.msgStore, sendersByPlatform, mgr, disp, s.execStore, s.memoryStore, s.sessionStore)
```

**目标代码**：

```go
beeMCPSrv := mcp.NewBeeServer(s.workerStore, mgr, s.taskStore, s.msgStore, sendersByPlatform, mgr, disp, s.execStore, s.memoryStore, s.sessionStore)
workerMCPSrv := mcp.NewWorkerServer(s.taskStore, s.msgStore, sendersByPlatform, s.memoryStore)
```

### 7.2 修改 `internal/app/app.go` — buildAPIServer 调用

**当前代码**（第 159 行）：

```go
srv, err := buildAPIServer(cfg.Server, cfg.Bee.MCP, s, mgr, logRegistry, mcpSrv, localChatHandler)
```

**目标代码**：

```go
srv, err := buildAPIServer(cfg.Server, cfg.Bee.MCP, s, mgr, logRegistry, beeMCPSrv, workerMCPSrv, localChatHandler)
```

### 7.3 修改 `internal/app/app.go` — buildAPIServer 签名

**当前代码**（第 244 行）：

```go
func buildAPIServer(serverCfg config.ServerConfig, mcpCfg config.MCPConfig, s appStores, mgr *worker.Manager, logRegistry *worker.ActiveLogRegistry, mcpSrv *mcp.MCPServer, localChat *api.LocalChatHandler) (*api.Server, error) {
```

**目标代码**：

```go
func buildAPIServer(serverCfg config.ServerConfig, mcpCfg config.MCPConfig, s appStores, mgr *worker.Manager, logRegistry *worker.ActiveLogRegistry, beeMCPSrv *mcp.MCPServer, workerMCPSrv *mcp.MCPServer, localChat *api.LocalChatHandler) (*api.Server, error) {
```

**函数体内调用 `api.NewServer` 的变更**：

**当前代码**（第 252 行）：

```go
return api.NewServer(s.workerStore, s.execStore, mgr, logRegistry, mcpSrv, mcpCfg.APIKey, webui.DistFS, localChat, authHandler, jwtMiddleware)
```

**目标代码**：

```go
return api.NewServer(s.workerStore, s.execStore, mgr, logRegistry, beeMCPSrv, workerMCPSrv, mcpCfg.APIKey, mcpCfg.WorkerAPIKey, webui.DistFS, localChat, authHandler, jwtMiddleware)
```

### 步骤 7 验证

```bash
go build ./...
```

---

## 步骤 8：测试

### 8.1 更新 `internal/mcp/tools_test.go` — 现有测试适配

**当前 `setupMCPServerWithMessaging` 函数**（第 20-40 行）已使用 `mcp.NewServer`。由于 `NewServer` 被保留为 `NewBeeServer` 的别名，现有测试无需修改即可通过。

### 8.2 新增 Worker Server 测试辅助函数

在 `internal/mcp/tools_test.go` 中新增：

```go
func setupWorkerMCPServer(t *testing.T) *mcp.MCPServer {
	t.Helper()
	db, err := store.InitDB(t.TempDir() + "/test.db")
	if err != nil {
		t.Fatalf("InitDB: %v", err)
	}
	t.Cleanup(func() { db.Close() })

	ts := store.NewTaskStore(db)
	ms := store.NewMessageStore(db)
	senders := make(map[string]platform.PlatformSenderAdapter)
	memStore := store.NewMemoryStore(db)
	return mcp.NewWorkerServer(ts, ms, senders, memStore)
}
```

### 8.3 新增测试用例 — 工具数量验证

```go
func TestBeeToolSchemasCount(t *testing.T) {
	schemas := mcp.ToolSchemas()
	if len(schemas) != 19 {
		t.Errorf("bee tool schemas: want 19 got %d", len(schemas))
	}
}

func TestWorkerToolSchemasCount(t *testing.T) {
	schemas := mcp.WorkerToolSchemas()
	if len(schemas) != 5 {
		t.Errorf("worker tool schemas: want 5 got %d", len(schemas))
	}
}
```

> 注意：需要在 `tools.go` 中导出 `WorkerToolSchemas`：
> ```go
> func WorkerToolSchemas() []toolSchema { return workerToolSchemas() }
> ```

### 8.4 新增测试用例 — Worker 无法调用 Bee 专用工具

```go
func TestWorkerCannotCallBeeTools(t *testing.T) {
	s := setupWorkerMCPServer(t)
	beeOnlyTools := []string{
		toolnames.ListWorkers,
		toolnames.GetWorker,
		toolnames.CreateWorker,
		toolnames.UpdateWorker,
		toolnames.DeleteWorker,
		toolnames.CreateTask,
		toolnames.ListTasks,
		toolnames.CancelTask,
		toolnames.ClearSession,
		toolnames.GetWorkerStatus,
		toolnames.GetSystemOverview,
		toolnames.ListBeeExecutions,
		toolnames.ListSessionContexts,
		toolnames.ClearWorkerSession,
	}
	for _, tool := range beeOnlyTools {
		_, err := s.CallTool(tool, mustMarshal(t, map[string]any{}))
		if err == nil {
			t.Errorf("worker should not be able to call %s", tool)
		}
		if !strings.Contains(err.Error(), "unknown tool") {
			t.Errorf("CallTool(%s): want 'unknown tool' error, got: %v", tool, err)
		}
	}
}
```

> 注意：需要在测试文件 import 中添加 `"strings"`。

### 8.5 新增测试用例 — Worker 可以调用允许的工具

```go
func TestWorkerCanCallAllowedTools(t *testing.T) {
	s := setupWorkerMCPServer(t)
	workerTools := []string{
		toolnames.MarkTaskComplete,
		toolnames.SendMessage,
		toolnames.SaveMemory,
		toolnames.GetMemory,
		toolnames.DeleteMemory,
	}
	for _, tool := range workerTools {
		// 调用时可能因参数不全返回业务错误，但不应返回 "unknown tool" 错误
		_, err := s.CallTool(tool, mustMarshal(t, map[string]any{}))
		if err != nil && strings.Contains(err.Error(), "unknown tool") {
			t.Errorf("worker should be able to call %s, got unknown tool error", tool)
		}
	}
}
```

### 步骤 8 验证

```bash
go test ./internal/mcp/... -v
go test ./... 2>&1 | tail -5
```

---

## 步骤 9：端到端验证

### 9.1 启动验证清单

1. 启动 openbee：`go run . serve`
2. 检查启动日志中不出现错误
3. 验证三组 MCP 端点均可访问：
   - `curl -H "X-API-Key: <bee_key>" http://localhost:8080/mcp/bee/sse` → SSE 连接
   - `curl -H "X-API-Key: <worker_key>" http://localhost:8080/mcp/worker/sse` → SSE 连接
   - `curl -H "X-API-Key: <bee_key>" http://localhost:8080/mcp/sse` → SSE 连接（兼容）

### 9.2 API Key 交叉验证

4. `curl -H "X-API-Key: <worker_key>" http://localhost:8080/mcp/bee/sse` → **401 Unauthorized**
5. `curl -H "X-API-Key: <bee_key>" http://localhost:8080/mcp/worker/sse` → **401 Unauthorized**

### 9.3 功能验证

6. 发送一条消息到任意已配置的平台
7. 验证 Bee 正常接收并分配任务
8. 验证 Worker 正常执行任务并通过 `send_message` 回复
9. 检查日志确认连接路径：
   - Bee: `MCP SSE connected` 日志来自 `/mcp/bee/sse`
   - Worker: `MCP SSE connected` 日志来自 `/mcp/worker/sse`

---

## 变更文件汇总

| # | 文件路径 | 变更类型 | 步骤 |
|---|---------|---------|------|
| 1 | `internal/config/config.go` | 修改 | 步骤 1 |
| 2 | `internal/config/config.yaml.tmpl` | 修改 | 步骤 1 |
| 3 | `internal/config/config_bee_test.go` | 修改 | 步骤 1 |
| 4 | `internal/claude/invoker.go` | 修改 | 步骤 2 |
| 5 | `internal/claude/invoker_test.go` | 修改 | 步骤 2 |
| 6 | `internal/mcp/tools.go` | 修改 | 步骤 3-4 |
| 7 | `internal/mcp/server.go` | 修改 | 步骤 4 |
| 8 | `internal/api/router.go` | 修改 | 步骤 5 |
| 9 | `internal/bee/bee_process.go` | 修改 | 步骤 6 |
| 10 | `internal/worker/manager.go` | 修改 | 步骤 6 |
| 11 | `internal/app/app.go` | 修改 | 步骤 1, 7 |
| 12 | `internal/mcp/tools_test.go` | 修改 | 步骤 8 |

共计 **12 个文件**变更，无新增文件。

---

## 依赖关系图

```
步骤 1 (config)
    │
    ├──→ 步骤 2 (invoker URL)
    │       │
    │       ├──→ 步骤 6 (bee_process + manager)
    │       │
    │       └──→ 步骤 7 (app assembly) ←─┐
    │                                     │
    ├──→ 步骤 3 (tool grouping)           │
    │       │                             │
    │       └──→ 步骤 4 (server split) ───┘
    │               │
    │               └──→ 步骤 5 (routing)
    │                       │
    │                       └──→ 步骤 7 (app assembly)
    │
    └──→ 步骤 8 (tests) ← 依赖步骤 3, 4
              │
              └──→ 步骤 9 (e2e verification) ← 依赖步骤 7
```

**建议执行顺序**：1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 9

步骤 2 和步骤 3 可以并行（不同文件，无代码依赖），但步骤 4 依赖步骤 3。
